# Hello mdx 

mdx in next.js app!

- one
- two
- three

```javascript
console.log(Hello)
```

Here is a random number: { Math.random() }

export default ({ children }) => <div className="prose">{children}</div>